{{ form_start(form, {'attr': {'class': 'myform'}}) }}
{% for child in form.children %}
    {{ form_label(child, null, {'label_attr': {'class': 'mylabel'}}) }}
    {{ form_errors(child) }}
    {{ form_widget(child) }}

{% endfor %}

{# <input type="submit" value="{{ submitButtonText|default('Create Pin') }}"/> #}
<input type="submit" class="btn_submit" value="{{ submitButtonText ?? 'Create Pin' }}"/>

{{ form_end(form, {'render_rest': false}) }}
